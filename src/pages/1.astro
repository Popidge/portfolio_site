---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import ProjectCard from '../components/ProjectCard.astro';
import { getCollection } from 'astro:content';

const projects = (await getCollection('projects')).sort((a, b) => a.data.order - b.data.order);

const experienceTimeline = [
  { year: '2024', title: 'AI-Native Developer', description: 'Freelance AI & Web Development' },
  { year: '2022', title: 'IT Support Specialist', description: 'Technical support and systems administration' },
  { year: '2019', title: 'VR Experience Operations', description: 'Managing immersive technology experiences' },
  { year: '2016', title: 'BBC Local Radio', description: 'Broadcasting and content production' },
  { year: '2014', title: 'UK Civil Service', description: 'Middle management and administration' },
  { year: '2012', title: 'BSc Chemistry', description: 'University of Leicester (Hons)' },
];
---

<BaseLayout title="Jamie Taylor — AI-Native Developer" design={1}>
  <div class="min-h-screen bg-[var(--bg-primary)]">
    <Navigation design={1} currentPath="/1" />
    
    <!-- Hero Section -->
    <header class="min-h-screen flex items-center px-8 pt-24">
      <div class="max-w-6xl mx-auto w-full">
        <div class="animate-fadeInUp">
          <p class="text-sm uppercase tracking-[0.3em] text-[var(--text-muted)] mb-8">
            AI & Web Development
          </p>
          
          <h1 class="font-[Instrument_Serif] text-5xl md:text-7xl lg:text-8xl text-[var(--text-primary)] leading-[0.9] mb-8">
            <span class="block">Hello, I'm</span>
            <span class="block italic">Jamie Taylor</span>
          </h1>
          
          <p class="max-w-2xl text-xl text-[var(--text-secondary)] leading-relaxed mb-12 animate-fadeInUp stagger-2">
            An AI-native developer from Loughborough, UK. I find creative ways to use AI 
            to build remarkable projects and push boundaries. My diverse background spans 
            chemistry, broadcasting, civil service, and VR operations — each experience 
            shaping how I approach problems today.
          </p>
          
          <div class="flex gap-8 animate-fadeInUp stagger-3">
            <a href="#work" class="group inline-flex items-center gap-2 text-sm uppercase tracking-widest text-[var(--text-primary)]">
              View Work
              <span class="w-8 h-px bg-[var(--text-primary)] group-hover:w-16 transition-all"></span>
            </a>
            <a href="#about" class="text-sm uppercase tracking-widest text-[var(--text-muted)] hover:text-[var(--text-primary)] transition-colors">
              About Me
            </a>
          </div>
        </div>
      </div>
    </header>
    
    <!-- Work Section -->
    <section id="work" class="py-32 px-8 bg-[var(--bg-secondary)]">
      <div class="max-w-6xl mx-auto">
        <div class="mb-20">
          <p class="text-sm uppercase tracking-[0.3em] text-[var(--text-muted)] mb-4">
            Selected Projects
          </p>
          <h2 class="font-[Instrument_Serif] text-4xl md:text-5xl text-[var(--text-primary)] italic">
            Recent Work
          </h2>
        </div>
        
        <div class="space-y-0">
          {projects.map((project, index) => (
            <ProjectCard project={project} design={1} index={index} />
          ))}
        </div>
      </div>
    </section>
    
    <!-- About Section -->
    <section id="about" class="py-32 px-8">
      <div class="max-w-6xl mx-auto">
        <div class="grid md:grid-cols-2 gap-20">
          <div class="animate-fadeInUp">
            <p class="text-sm uppercase tracking-[0.3em] text-[var(--text-muted)] mb-4">
              Background
            </p>
            <h2 class="font-[Instrument_Serif] text-4xl md:text-5xl text-[var(--text-primary)] italic mb-8">
              The Journey
            </h2>
            
            <div class="space-y-6 text-[var(--text-secondary)]">
              <p>
                My path to AI development wasn't linear — it was shaped by curiosity and a drive 
                to understand how things work. A chemistry degree taught me systematic thinking 
                and attention to detail.
              </p>
              <p>
                Years in the civil service showed me how organizations function (and sometimes 
                don't). BBC Local Radio sparked my love for storytelling and audience engagement. 
                VR operations revealed the magic of immersive technology.
              </p>
              <p>
                Now, as an AI-native developer, I bring all these perspectives together. I see 
                AI not as a replacement for human creativity, but as an amplifier — a tool 
                that lets us achieve things we couldn't alone.
              </p>
            </div>
          </div>
          
          <div class="animate-fadeInUp stagger-2">
            <p class="text-sm uppercase tracking-[0.3em] text-[var(--text-muted)] mb-4">
              Experience
            </p>
            <h2 class="font-[Instrument_Serif] text-4xl md:text-5xl text-[var(--text-primary)] italic mb-8">
              Timeline
            </h2>
            
            <div class="space-y-8">
              {experienceTimeline.map((item) => (
                <div class="flex gap-8 group">
                  <span class="text-sm text-[var(--text-muted)] font-mono w-16 shrink-0">
                    {item.year}
                  </span>
                  <div>
                    <h3 class="text-[var(--text-primary)] group-hover:italic transition-all">
                      {item.title}
                    </h3>
                    <p class="text-sm text-[var(--text-muted)]">
                      {item.description}
                    </p>
                  </div>
                </div>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
    
    <!-- Philosophy Section -->
    <section class="py-32 px-8 bg-[var(--bg-secondary)]">
      <div class="max-w-4xl mx-auto text-center">
        <blockquote class="font-[Instrument_Serif] text-3xl md:text-5xl text-[var(--text-primary)] italic leading-snug mb-8">
          "The best technology disappears — it becomes invisible, letting 
          the human experience shine through."
        </blockquote>
        <p class="text-sm uppercase tracking-[0.3em] text-[var(--text-muted)]">
          — My Design Philosophy
        </p>
      </div>
    </section>
    
    <Footer design={1} />
  </div>
</BaseLayout>
